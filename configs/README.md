# Configs

This folder contains sanitized configuration snapshots from the Raspberry Pi travel router project.

Sensitive values were removed or redacted before publication, including:

- private keys
- preshared keys
- public IP addresses
- MAC addresses
- passwords
- unique identifiers

These files are included to document the project’s technical implementation without exposing operational secrets.
